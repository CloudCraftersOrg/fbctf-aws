owner = "davismar98"
# ui_allow_cidr = "203.0.113.4/32"   # your IP, to hit :5000 directly; default is SSM-only
max_lifetime_minutes = 300   # 0 disables auto-shutdown (boxes stay up until `make destroy`)
